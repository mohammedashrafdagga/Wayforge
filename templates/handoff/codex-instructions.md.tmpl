# Codex: review the {{FEATURE_SLUG}} implementation

You are picking this up cold, with no memory of the Claude Code or Cursor sessions that produced this code. This file is self-contained.

## What to do

1. Read `{{PLAN_PATH}}` in full — every task's acceptance criteria is what you're checking against.
2. Diff the branch `{{BRANCH_NAME}}` against its base to see what was actually implemented.
3. Read `{{ARCHITECTURE_PATH}}` and `{{CONVENTIONS_PATH}}` and confirm the implementation respects them — dependency direction, feature-folder placement, async conventions — even where the acceptance criteria alone might technically pass with a violation.
4. For each task in the plan, mark each acceptance criterion pass/fail with a one-line reference to where you confirmed it (file:line).
5. Write your findings to `{{REVIEW_PATH}}` following `templates/review.md.tmpl`'s structure.
6. Do not edit `workflow/data-model.md`, `workflow/api-registry.md`, or `workflow/user-stories.md` directly. If the review passes and a human/agent runs the merge step, that happens via `${CLAUDE_PLUGIN_ROOT}/scripts/merge_master_doc.py` and `validate_docs.py`, not a manual edit.

## Files referenced

- Plan: `{{PLAN_PATH}}`
- Review output (write here): `{{REVIEW_PATH}}`
- Architecture constitution: `{{ARCHITECTURE_PATH}}`
- Conventions: `{{CONVENTIONS_PATH}}`
