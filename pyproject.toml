[project]
name = "wayforge-cli"
version = "0.1.0"
description = "Wayforge — a spec-driven, multi-agent development CLI. Bootstraps a project with living docs (architecture constitution, data model, API registry, user stories) and installs /wayforge-* skills for Claude Code, Cursor, and Codex."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.12",
    "rich>=13.0",
]

[project.scripts]
wayforge = "wayforge_cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wayforge_cli"]

[tool.hatch.build.targets.wheel.force-include]
# Bundle the shared docs/scripts payload so `wayforge init` works from an
# installed wheel (uv tool install / uvx) without needing the source repo
# checked out alongside it.
"templates" = "wayforge_cli/core_pack/templates"
"scripts" = "wayforge_cli/core_pack/scripts"
"skills" = "wayforge_cli/core_pack/skills"
